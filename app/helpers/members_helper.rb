module MembersHelper

    # def user_joined_in_group?
    #     if user_signed_in?
    #       !Role.where(user_id: current_user.id, group_id: params[:group_id], status: 1).blank?
    #     end
    #   end
    
    #   def user_pending_in_group?
    #     if user_signed_in?
    #       !Role.where(user_id: current_user.id, group_id: params[:group_id], status: 2).blank?
    #     end
    #   end
      
end
